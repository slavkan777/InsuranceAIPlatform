namespace InsuranceAIPlatform.Services.AiAnalysis.Contracts;

/// <summary>
/// Forward-declaration marker for the AI Analysis service's future run contract.
/// Id-only placeholder — no findings, no prompt text, no PII — formalised in a later AI gate.
/// </summary>
public record AiRunRef(string RunId);
